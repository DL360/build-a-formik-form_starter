declare const defaultRules: {
    numbers: RegExp;
    email: RegExp;
    required: RegExp;
    hasNumber: RegExp;
    hasUpperCase: RegExp;
    hasLowerCase: RegExp;
    hasSpecialCharacter: RegExp;
    date(format: string | undefined, value: string): boolean;
    minlength(length: number, value: string): boolean;
    maxlength(length: number, value: string): boolean;
    equalPassword(dataToCompare: string, value: string): boolean;
};
export default defaultRules;
